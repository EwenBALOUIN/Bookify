import { createTheme } from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#6c0263',
    darkGrey: '#393E40',
    grey: '#9A98A0',
    primaryGradient: '#6c02639d',
  },
  darkColors: {
    primary: '#000',
  },
});

export default theme;
